plugins {
    id 'io.github.pacificengine.build.repository'
    id 'io.github.pacificengine.build.zip'
    id 'io.github.pacificengine.build.release'
}

dependencies {
    toCopy project(':instance')
}